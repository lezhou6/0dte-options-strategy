notebooks/ contains some early exploration of the data. 

Add a creds.txt with Theta Data username and id in notebooks to use thetadata to fetch data. 

Raw data is fetched and stored in data/raw as parquet. 